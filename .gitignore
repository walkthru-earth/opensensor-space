.evidence/template
.svelte-kit
build
node_modules
.DS_Store
static/data
*.options.yaml
.vscode/settings.json
.env
.evidence/meta
.windsurfrules

# This project uses Bun. Prevent accidental pnpm lockfile commits.
pnpm-lock.yaml

# content/stations/ is synced from the contributions branch during deploy.
# It should never be committed on main.
content/

# Nested CLAUDE.md files are autogenerated per-directory by claude-mem.
# The root CLAUDE.md IS checked in (project memory).
CLAUDE.md
!/CLAUDE.md
